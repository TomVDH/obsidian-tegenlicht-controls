// src/main.ts — placeholder; replaced in Task 4
import { Plugin } from "obsidian";
export default class TegenlichtControlsPlugin extends Plugin {
  async onload() { console.log("Tegenlicht Controls loaded"); }
}
